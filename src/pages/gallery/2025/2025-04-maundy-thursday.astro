---
import Layout from '../../../layouts/Layout.astro'
import Gallery from '../../../components/Gallery.svelte'

const metaData = {
	title: 'Maundy Thursday 2025',
	desciption: 'Maundy Thursday 2025',
}

const amountOfImages = 43; 

let images = []

for (let i = 0; i <= amountOfImages; i++) {
	images.push({
		largeURL: `${
			import.meta.env.PUBLIC_ASSETS
		}images/2025/2025-04-maundy-thursday/${i}.jpg`,
		thumbnailURL: `${
			import.meta.env.PUBLIC_ASSETS
		}images/2025/2025-04-maundy-thursday/thumbnails/${i}.JPG`,
		width: 1200,
		height: 800,
	})
}
---

<Layout {...metaData}>
	<h1>{metaData.title}</h1>
	<Gallery
		client:load
		galleryID='maundy-thursday-2025-04'
		images={images}
	/>
</Layout>
